#pragma once
#include"DxLib.h"

const VECTOR TittleTextPos = VGet(550, 60, 0);

class UIManager
{
public:
    UIManager();
    ~UIManager();
    void DrawTittleSece();  // タイトルシーン描画
private:
    
};